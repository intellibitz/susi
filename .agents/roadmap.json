{
  "description": "Structural roadmap and future evolution goals of the susi substrate (agent governance \u2014 not end-user docs).",
  "schema": "susi/roadmap/v1",
  "strategic_priority": "Federation",
  "target_version": "0.2.0",
  "title": "SUSI Substrate Roadmap: The Sovereign Will",
  "vectors": [
    {
      "id": "VC-200-001",
      "mastery_target": "P2P Non-blocking Swarm",
      "progress": "PARTIAL: UDP LAN discovery, TCP JSON-RPC peer dispatch, and trust-score ranking already worked. Closed the routing gap the code itself flagged (\"bloom-filters of peer registries for perfect routing\"): peers now exchange a real 256-bit `CapabilityBloom` of their registered tool names during ping/pong, and `rank_peers_for_goal` scores peers by goal/capability overlap instead of trust alone (`src/gawd/amas.rs`). As of 2026-09-22 (EV-2022922-101), added a real quorum-commit primitive: `SusiSupervisor::quorum_majority` (`src/gawd/amas.rs`) checks the shared mission blackboard \u2014 which already holds both local agent outputs and dispatched peer-node outputs under `PeerNode_<id>` keys \u2014 for a strict majority of independently-produced entries that exact-match after normalization, and commits that value directly (a new `QUORUM_COMMIT` A2A action, distinct from the pre-existing `STATE_CONVERGENCE` rank-leader/LLM-synthesis paths) instead of only ever picking one rank-leader's answer or asking an LLM to reconcile. Remaining gap narrowed (cluster-key membership landed): peers now exchange an HMAC-SHA256-signed ping/pong (`SUSI_PING_SIG`/`SUSI_PONG_SIG`, susi-peer-v1) keyed by a shared 32-byte secret at ~/.susi/cluster.key (0600, `susi_config::cluster_key`); a pong must echo the requester's nonce, so a spoofed or replayed UDP answer can never promote a peer past `PeerAdmission::Discovered`. Verified peers persist in ~/.susi/peers.json and rehydrate as `Explicit` on restart. As of 2026-09-23, the quorum vote tolerates mid-vote churn via a pinned electorate (the Raft configuration-entry concept applied to one consensus round): supervise_mission snapshots the voter set at broadcast \u2014 local fleet names + PeerNode_<id> keys for every Explicit peer actually dispatched \u2014 and quorum_majority counts only pinned members, thresholding against the electorate's full size rather than the respondents. A peer dying mid-vote now shrinks the response set instead of silently lowering the quorum bar, and a peer admitted after the broadcast can never tilt the count. Commit replication landed (kernel-ABI commit_log module, vendored to all planes): every QUORUM_COMMIT is sealed into a CommitRecord (pinned electorate, tally, quorum threshold, SHA-256 value hash, HMAC-SHA256 signature under cluster.key), appended to ~/.susi/commit_log.jsonl, and pushed to each voting peer via the governed commit_record tool \u2014 receivers verify signature + quorum consistency before appending, so forged or below-quorum records fail closed. A coordinator crash mid-round no longer loses the decision, and commit_log exposes the ledger for audit. Leader election + per-coordinator ordering landed: SusiSupervisor::elect_leader is a deterministic bully election over the verified roster (highest trust, node_id tie-break) \u2014 every member converges on the same leader without a round-trip, and Discovered/inactive nodes can never lead. CommitRecord carries seq (per-coordinator monotonic, signed) and leader (elected at commit time); the commit_record receiver runs missing_seqs gap detection and surfaces replication gaps in the tool response, and susi commits shows SEQ per row. Anti-entropy repair landed: commit_record receivers that detect a seq gap resolve the coordinator's address through the new gawd.cluster.peers roster topic and pull missing records via commit_log_fetch, verifying each signature before append \u2014 lost replication now self-heals, and a peer that can't reach the coordinator surfaces the gap rather than hiding it. Append is now idempotent with equivocation detection: re-delivered records no-op (replication retries can't corrupt the count-based seq assignment), and a different cluster-signed record claiming an already-held (coordinator, seq) slot is refused as equivocation rather than silently ordering both. Term ordering landed: ~/.susi/term.json holds {term, leader}, bumped by claim_leadership on leader transitions and signed into every CommitRecord; commit_record rejects stale-term pushes (Raft's AppendEntries term check), adopts newer terms (step-down rule), and flags same-term leader conflicts \u2014 terms gate new writes while anti-entropy still fills old-term history. State-machine replay landed: commit_log::replay() folds the ledger into ClusterState (max term, leader, per-coordinator high-water seqs, anomalies) so any node holding the same records derives the same consensus view, and susi commits audit/replay plus the new `susi os` unified substrate view surface it to operators. What remains vs full Raft/Paxos: log-matching prevLogIndex/term check landed (each CommitRecord signs prev_epoch \u2014 its predecessor's epoch \u2014 and receivers refuse a divergent link as fork evidence while replay flags residual chain-breaks); Membership-change consensus landed 2026-09-24: roster deltas are committed to the replicated ledger as leader-signed `member_add`/`member_remove`/`member_unban` records (CommitRecord.kind \u2014 Raft's config-entry analog, sealed by susi peers add/remove/unban, pushed through the commit_record tool path, and applied to peers.json/peers_banned.json on append since the ledger IS applied state). Every node also gained a stable persisted identity (~/.susi/node_id, wire_node_id()) \u2014 previously all members shared the hardcoded susi-daemon-node id, collapsing per-coordinator seq namespaces and making elections non-deterministic; signed pings now carry the real node_id (7-field wire, 6-field back-compat + peers-add legacy fallback). Replay folds membership deltas into ClusterState.roster/banned so identical logs derive identical membership. Leader-proposed membership landed 2026-09-24: privileged records (member deltas, both rekey phases) are only applied when sealed by a coordinator claiming its own leadership (record.leader == record.coordinator) \u2014 Raft's leader-proposed configuration-entry rule; non-leader members delegate member_add through the member_propose tool. Ledger compaction landed the same day: commit_log::compact() folds the live ledger into commit_snapshot.json (high-water marks + derived state) and archives pre-snapshot records to commit_log.archive.jsonl, retaining per-coordinator anchor records so sealing/anti-entropy stay snapshot-unaware; replay seeds from the snapshot so compacted and uncompacted views are identical, commit_log_fetch + anti-entropy serve the archive, and below-floor arrivals dedup against it (identical \u2192 no-op, divergent \u2192 equivocation). Auto-compacts in the scout sweep at >2048 records; susi commits compact is the manual form. What remains vs full Raft/Paxos: no commit-index/apply phase (the ledger IS the applied state \u2014 a deliberate simplification, not a gap), and no true joint-consensus for membership transitions: under a shared symmetric key, multi-signatures are cryptographically meaningless (every member signs identically), so Raft's C_old,new safety needs per-member identity (PKI) to mean anything \u2014 leader-proposal + term gates bound a stale leader's divergence but cannot fully serialize overlapping config changes. The honest next step toward it was per-member signing keys \u2014 now landed (EV: tests/member_signing.rs): every node holds an Ed25519 identity at ~/.susi/node.key (0600); every sealed record carries member_sig (signature over the record HMAC), and a v2 handshake dialect (SUSI_PING_SIG2/PONG_SIG2, tried first with v1 fallback) attests each responder's pubkey so rosters bind member\u2192key on verified pong or committed member_add (member_pubkey, first-write-wins, key_bound_at=record.committed_at). Once bound, intake refuses records from that coordinator without a valid member_sig \u2014 a stolen cluster.key can no longer mint records as a bound member; pre-binding history still converges (committed_at < key_bound_at). Residuals documented in ARCHITECTURE.md: the pre-binding window, backdated committed_at, and proposer-attested member_pubkey are bounded but nonzero until binding is itself consensus-checked \u2014 the remaining step toward genuine joint consensus.",
      "type": "SWARM",
      "vector": "Federated Cross-Node Consensus"
    },
    {
      "id": "VC-200-002",
      "mastery_target": "Autonomous Trait Patching",
      "progress": "PARTIAL, reinterpreted: literal runtime Rust trait patching isn't achievable in safe Rust. Delivered the real equivalent \u2014 `resolve_capability_gap` now falls back to `ReflexSynthesizer::synthesize_wasm_reflex`, which generates and compiles an input-dependent WASI reflex and hot-loads it via the existing `reflex_<name>` convention with **no daemon restart** (`src/gawd/reflex_synth.rs`, `src/gmcp/tools/mod.rs`). Also fixed two latent bugs found while building this: the target triple (`wasm32-wasi`, removed from rustc) was corrected to `wasm32-wasip1`, and the synthesized source previously had no `fn main()`/`_start`, so it could never have executed even if it had compiled. Compilation requires the `wasm32-wasip1` rustup target; the live end-to-end test (`test_wasm_reflex_hot_patch_end_to_end`) honestly skips (not silently) when that target is absent rather than assuming success. As of 2026-09-22 (EV-2022922-099), that target IS installed in this dev environment and the test was run directly (not just checked for the skip path): it took the real compile-and-execute branch and passed, `rustc --target wasm32-wasip1` compiling the synthesized reflex and `WasmHost::execute_reflex` running it under WASI with correct output. This confirms the full live path genuinely works, at least in one environment with the toolchain present \u2014 it is still environment-dependent (unverified whether `wasm32-wasip1` is installed in CI or other dev machines), and this run only exercised `synthesize_wasm_reflex` + `WasmHost::execute_reflex` directly, not a full pass through `resolve_capability_gap`'s own call site.",
      "type": "CORE",
      "vector": "Neural Self-Repair & Hot-Reload"
    }
  ],
  "version": "0.12.0"
}