{
  "schema": "susi/identity/v1",
  "version": "0.9.0",
  "title": "SUSI Substrate Identity",
  "description": "This ledger defines the engine's constitutional mandates, component\ntopology, execution model, and release/runtime protocols. It is the source\nof truth compiled into the binary at build time (see `susi-gawd-agents build.rs → AlphaSelf`).\nThe public host contract in `README.md` must stay consistent with this document\nand with `susi_paths::ports` / `SusiDaemon` — when they disagree, source wins\nand both documents are updated in the same change.",
  "audience": "agent-governance",
  "canonical_addressing": "Canonical addressing: mandates in this document are cited as `Mandate N` (Pillar I) or `Pillar <N> item N` (Pillars II–IV). Source comments predating this document's current structure used to also cite `Rule N`, `Aspiration N`, and `ENGINE-N` — none of those schemes ever corresponded to this document's current numbering, and no reconciliation table exists. As of 2026-09-18 those stale citations were reconciled across every file that carried them. This happened in two passes, and the first pass's own \"zero remain\" claim was itself wrong: a case-sensitive sweep found 96 mixed-case `Rule N`/`Aspiration N`/`ENGINE-N` occurrences across 19 files and fixed all of them, but the sweep's own regex only matched `Rule` (not `RULE`), so it silently missed 17 further all-caps `RULE N` occurrences across 10 files — caught and fixed in a second pass once discovered. Combined, every citation was either stripped down to its still-accurate descriptive text (the numbered scheme dropped, the reasoning kept — the large majority) or, in the handful of cases where the reference was verifiably the same concept under a new name, rewritten to its real current citation: `admin.rs`'s and `evolution.rs`'s/`reflex_synth.rs`'s \"Motion Rule (Protocol)\" citations to `Pillar IV item 3` and `Mandate 20` respectively (two genuinely distinct concepts that historically shared the same name, per Mandate 20's own collision note), and `security.rs`'s/`net_guard.rs`'s exact-title matches to `Mandate 10`/`Mandate 12`. No correspondence was ever invented without a verifiable match — where a plausible-looking old number didn't match a current mandate's substance, the number was dropped rather than guessed. Two test-fixture strings in `amas.rs` containing the literal words \"test aspiration 23\" (simulated mission-goal text, not code comments) were correctly left untouched by both passes.",
  "citation_drift_note": "A related, distinct kind of drift — comments citing `Mandate N` using the *current* terminology with a number that doesn't match this document's actual current mandate at that number — was scoped and fixed separately (`EV-2022920-031`): 5 of 41 such citations were wrong, corrected or stripped the same way.",
  "host_contract": {
    "ports": [
      {
        "port": 9090,
        "surface": "GMCP / MCP HTTP (`/mcp`; `/messages` alias)"
      },
      {
        "port": 9091,
        "surface": "GEMI HTTP (inference / models)"
      },
      {
        "port": 9092,
        "surface": "A2A UDP discovery"
      },
      {
        "port": 9093,
        "surface": "GMCP HTTP alias (streamable / SSE)"
      }
    ],
    "notes": [
      "**`global susi`** — background daemon bound to the host substrate (`~/.susi` / `SusiDirs::substrate_home`), not to a project folder. Owns ports, models, and lock state.",
      "**`susi` CLI** — jailed to the caller's cwd; intents run against that workspace.",
      "**Canonical binary** — `~/.susi/bin/susi` (hot-reloads when `binary.hash` changes).",
      "**Control plane** — `susi start` / `susi stop` / `susi restart` are deterministic host commands (never natural-language missions). `start` / `restart` wait until ports 9090–9093 answer and print the endpoints.",
      "Polluted `~/.susi/config.json` port fields are ignored; accessors always return the constants above.",
      "Peer scouts must not bind 9092 — only the daemon owns the host-contract UDP port."
    ]
  },
  "foundation_pillars": [
    {
      "id": 1,
      "title": "Truth",
      "summary": "`TruthTransformer` / `SusiTruthAgent`; models never certify facts."
    },
    {
      "id": 2,
      "title": "Evidence",
      "summary": "`EvidenceSession` / `ToolReceipt` / `EvidenceRecord`; no naked assertions."
    },
    {
      "id": 3,
      "title": "Swarm",
      "summary": "multi-agent consensus (`susi-gawd`) agent-of-agents orchestrator."
    },
    {
      "id": 4,
      "title": "Blackboard",
      "summary": "`MissionBlackboard`; `.susi/last_blackboard.json` (`susi blackboard`)."
    },
    {
      "id": 5,
      "title": "Glass box",
      "summary": "Mandate 26 (not naked secrets); traces + blackboard + governance + crown reports + `.susi/receipt_archive.jsonl` (audit-only; truth uses live ledger); secrets redacted (Mandate 38)."
    },
    {
      "id": 6,
      "title": "Audit",
      "summary": "append-only HMAC-SHA256 chain (`audit_chain::verify_chain`)."
    },
    {
      "id": 7,
      "title": "Zero-config Auto",
      "summary": "Mandate 44; packs/MCP/models/peers auto-prime (`susi auto`); host installs/keys still gate cloud."
    },
    {
      "id": 8,
      "title": "Governance-first",
      "summary": "Mandate 37; Safety/Security before parallel fleet (`.susi/last_governance.json`)."
    },
    {
      "id": 9,
      "title": "Pluggable",
      "summary": "`CapabilityRegistry` + extension packs; end-to-end managed catalogs (`susi models` / `frontier` / `openweight` / `agents` / `frameworks` / `mcp` / `openrouter`)."
    },
    {
      "id": 10,
      "title": "Sandbox",
      "summary": "Wasmer `WasmHost::execute_untrusted_wasm`; Docker `sandbox_exec` when available."
    },
    {
      "id": 11,
      "title": "Host contract",
      "summary": "fixed ports 9090–9093; never silent drift."
    },
    {
      "id": 12,
      "title": "Reflexes",
      "summary": "Wasm under data dir; training threshold from config."
    },
    {
      "id": 13,
      "title": "Provision",
      "summary": "`bootstrap_zero_config_substrate` + Candle/weight ladder."
    },
    {
      "id": 14,
      "title": "Concurrency-first",
      "summary": "Tokio / Rayon / Crossbeam / parking_lot (Mandates 5, 28–30; lock-preferring, not wait-free absolute)."
    }
  ],
  "design_principles": [
    {
      "id": 1,
      "title": "Autonomous by default",
      "summary": "Mandate 46; plan and execute via swarm consensus, not step-by-step babysitting. Bare `susi \"<intent>\"` / `susi automate` dispatches GAWD end-to-end; callers are not prompted for each agent step. (One-time cloud-provider preference prompts are host config, not mission babysitting.) Autonomy stays evidence-gated and governance-first — not unbounded \"100% automation.\""
    },
    {
      "id": 2,
      "title": "Grounded outputs",
      "summary": "claims checked against tools and workspace state. Mission finals require live `ToolReceipt` citations or other absolute truth sources (`TruthTransformer`); EpistemicAuditor rejects hallucinated paths and failed `EvidenceRecord` trails."
    },
    {
      "id": 3,
      "title": "Traceable reasoning",
      "summary": "Mandate 26; thinking and tool calls are inspectable. Mission reports expose agent interactions + evidence ledger + blackboard in protocol output and persist `.susi/last_mission_trace.json` / `.susi/last_blackboard.json` (hashes/provenance for tools; no secret bodies — Mandate 38); tool receipts also append to `.susi/receipt_archive.jsonl` (hashes/provenance only — not truth authority). Not unbounded \"100% transparency.\""
    },
    {
      "id": 4,
      "title": "Concurrency-first",
      "summary": "Tokio, Rayon, Crossbeam, parking_lot on real hardware (Mandates 5, 28–30). Swarm dispatch uses Rayon work-stealing; daemon/HTTP use Tokio; pulse queue uses Crossbeam `SegQueue`; shared state prefers `parking_lot` / DashMap — lock-preferring, not a \"zero blocking forever\" claim."
    },
    {
      "id": 5,
      "title": "Rust substrate",
      "summary": "First-party runtime is Rust (or Wasm from Rust). Non-Rust processes are opt-in managed peers only (Mandate 43) — not a false \"100% Rust process tree\" claim."
    },
    {
      "id": 6,
      "title": "Zero-config auto",
      "summary": "Boot auto-primes packs/MCP/models/peers without hand-edited config for the happy path (Mandate 44). Installs and API keys remain explicit gates — not \"never configure anything.\""
    },
    {
      "id": 7,
      "title": "OS layer for AI agents",
      "summary": "Agent-of-agents substrate on the host machine (Mandate 45) — not a replacement host OS."
    },
    {
      "id": 8,
      "title": "One-line install",
      "summary": "Unix primary surface is `curl …/install.sh | bash` (Mandate 47); Binary Dominance + PATH inject + canonical `~/.susi/bin/susi`. Not \"always frictionless.\""
    }
  ],
  "pillars": {
    "dna": {
      "id": 1,
      "name": "THE DNA",
      "subtitle": "Constitutional Mandates",
      "mandates": [
        {
          "id": 1,
          "title": "No Lies",
          "imperative": "Never lie about what happened. Self-reported status, execution outcomes, test results, and known limitations must always match reality, even when the truth is a failure."
        },
        {
          "id": 2,
          "title": "No Hallucinations",
          "imperative": "Never fabricate a fact, API, file, or code reference that wasn't verified by reading it. An unverified claim must be stated as unverified, not asserted."
        },
        {
          "id": 3,
          "title": "Bloat Rejection (Continuous Audit)",
          "imperative": "Continuously audit for and eliminate technical bloat (dead code, oversized functions, unjustified `.unwrap()`/`.clone()` density, config/behavior drift). `BloatAuditor` (`susi bloat-audit`) is the enforcement mechanism, not a formality. A bare \"100% bloat-free forever\" claim is forbidden by Mandate 1 — this mandate is continuous rejection via measurable audit, not an unverifiable absolute clean state."
        },
        {
          "id": 4,
          "title": "Sub-2ms Client Reflex (Ack, Not Mission Done)",
          "imperative": "A *client* (CLI caller, IDE, HTTP/MCP caller) must never be left waiting past 2ms for acknowledgment — this governs the client-facing response, not the total wall-clock time of the work itself; see Mandate 32 for how that's achieved (instant return, streaming, or an async task handle). Internal primitive operations (a lock acquisition, a config read, a hashmap lookup) should stay sub-millisecond, but a multi-agent swarm mission's true completion time is bounded by inference and I/O, not this mandate — measuring end-to-end mission latency against a 2ms ceiling produces guaranteed, meaningless \"violation\" telemetry on every non-trivial mission and must not be done (see Mandate 26)."
        },
        {
          "id": 5,
          "title": "Lock-Preferring Swarm Path (Not Zero Blocking)",
          "imperative": "On the swarm execution path, prefer non-blocking concurrency: atomics, lock-free structures (`DashMap`), channels (Crossbeam/CSP), and `parking_lot` only when a mutex is required. Blocking `std::sync` locks and long holds on the Rayon/Tokio critical path are discouraged. Exceptions are allowed when labeled and bounded (e.g. `tokio::task::spawn_blocking` for sync I/O, short critical sections). A bare \"blocking locks prohibited everywhere / zero blocking ever\" claim is forbidden by Mandate 1 — this mandate is lock-*preferring* swarm design, not a proof of a fully wait-free process."
        },
        {
          "id": 6,
          "title": "Brutally Honest & Critical",
          "imperative": "Maintain a highly critical evaluation mode for code, architecture, and logic."
        },
        {
          "id": 7,
          "title": "Continuous Re-Verification",
          "imperative": "A fact verified once (a file's contents, a config value, a running process's state) can go stale mid-task. Re-check assumptions against current codebase and runtime state before acting on them, especially after any action that could have changed that state."
        },
        {
          "id": 8,
          "title": "Epistemic Chain of Truth",
          "imperative": "Source code and empirical runtime results outrank prior training knowledge, memory, or assumption. When they conflict, the freshly-read source or the freshly-run result wins, and prior belief is discarded, not defended. Mission completion requires absolute evidence only: live `EvidenceSession` / `ToolReceipt` citations, compiled binary reads, or native verified receipts — models never certify facts (`TruthTransformer`)."
        },
        {
          "id": 9,
          "title": "Professional Technical Standard",
          "imperative": "Keep code, documentation, and comments professional and technical — prefer precise, dense prose over marketing filler. A bare \"zero filler forever\" claim is forbidden by Mandate 1 (filler is subjective and not mechanically measurable); this mandate is a style target enforced by review and Mandate 6, not a count of zero adjectives."
        },
        {
          "id": 10,
          "title": "No Secret Leaks (Pattern Redaction, Not Omniscience)",
          "imperative": "Do not leak tokens, credentials, API keys, or sensitive configuration in logs, telemetry, mission traces, or outbound tool content. Durable records must pass Mandate 38 (`SecurityDetector::redact`) before write. A bare \"zero tolerance / never leak any secret shape\" claim is forbidden by Mandate 1 — detection is pattern- and config-based, not omniscient; unknown secret formats may require config updates. Intentional non-leak plus redaction is the mandate, not perfect cryptography of every possible secret."
        },
        {
          "id": 11,
          "title": "Real Working Code (Not Silent Fakes)",
          "imperative": "Shipped production paths must be functional — no hard-coded mockups or placeholders that present as real success. Tests, fixtures, stubs, and harness doubles are allowed when labeled as such (`#[cfg(test)]`, clear stub names). A bare \"no placeholders anywhere in the repo\" claim is forbidden by Mandate 1; this mandate forbids silent fake-success in operator-facing and daemon paths (aligned with Mandate 44 diagnosable gates)."
        },
        {
          "id": 12,
          "title": "Hardware Authority (Bounded by Governance Too)",
          "imperative": "Physical hardware capacity bounds compute and memory: saturate CPU/GPU topologies responsibly and prevent OOM proactively. Hardware is **not** the sole limit — rate limits, bearer auth (Mandate 40), governance (Mandate 37), evidence gates, and operator prefer pins also constrain work. A bare \"hardware is the only ceiling\" claim is forbidden by Mandate 1."
        },
        {
          "id": 13,
          "title": "Daemon Persistence",
          "imperative": "Sustain a continuous, resilient background process bound to the host substrate, owning the hard-contract ports 9090–9093 (GMCP/GEMI/UDP/alias) plus models and lock state. Never silently remap those ports."
        },
        {
          "id": 14,
          "title": "Substrate Priming (Policy-Gated)",
          "imperative": "Autonomously provision the highest-ranking open-source neural weights compatible with detected hardware **when policy allows** (`bootstrap_zero_config_substrate` / model ladder), honoring prefer pins, no-download / offline flags, and disk limits. A bare \"always download the absolute highest weights\" claim is forbidden by Mandate 1 — priming is best-effort under Mandate 44 gates, not forced saturation of every host."
        },
        {
          "id": 15,
          "title": "Registry Hot-Reload",
          "imperative": "Validate the dynamic agent registry via timestamp-based audits to prevent stale behavior injection."
        },
        {
          "id": 16,
          "title": "Empirical Adaptation (Governance-Bounded)",
          "imperative": "Adapt runtime structure and agent composition from empirical swarm outcomes (registry hot-reload, reflex synthesis, mission feedback) — not from unconstrained invention. Adaptation remains bounded by Mandates 37 (governance-first), 41 (untrusted input), 25/36 (destructive guards), and 46 (autonomy under evidence). A bare \"Absolute Sovereignty / unconstrained synthesis\" claim is forbidden by Mandate 1."
        },
        {
          "id": 17,
          "title": "Recursive Decomposition (When Beneficial)",
          "imperative": "Prefer spawning parallel sub-swarms for separable atomic components and joining on convergence when that improves throughput or clarity. Sequential or single-agent dispatch remains correct for small goals, governance-only checks, or when parallelism would add noise. A bare \"always parallel sub-swarms\" claim is forbidden by Mandate 1."
        },
        {
          "id": 18,
          "title": "Mission Persistence (Best-Effort Checkpoints)",
          "imperative": "Persist mission-relevant state (blackboard, traces, receipts, neural/mission checkpoints where implemented) so interrupted work can resume or be diagnosed. A bare \"universal crash-proof recovery of every swarm\" claim is forbidden by Mandate 1 — persistence coverage is best-effort and path-dependent; missing checkpoints must fail honestly, not invent resumed success."
        },
        {
          "id": 19,
          "title": "Decoupled Messaging (Prefer Async)",
          "imperative": "Prefer asynchronous messaging (Pub-Sub, Actor/CSP channels, Crossbeam queues) for multi-agent orchestration. Short synchronous local calls remain allowed on hot paths where a channel would be pure overhead. A bare \"all messaging must be async\" claim is forbidden by Mandate 1."
        },
        {
          "id": 20,
          "title": "Alpha-Self Evolution Order",
          "imperative": "Self-modification workflow strictly follows: Motion (the triggering intent) -> Architecture (design) -> Structure (implementation) -> Logic (verification). Not to be confused with the release \"Motion Rule\" (Pillar IV item 3), a distinct, unrelated CI sequence that happens to share the word \"Motion\" — the two were previously both called \"The Motion Rule,\" a naming collision that made this document self-contradictory about what \"The Motion Rule\" means."
        },
        {
          "id": 21,
          "title": "Dynamic Intent Resolution",
          "imperative": "Resolve operator intents via dynamic registries, config, and control-plane routing (`CapabilityRegistry`, extension packs, agent/framework/MCP/model planes) rather than hard-coded goal enums. A bare \"Continuous Intent Manifold\" slogan without a defined substrate mechanism is forbidden by Mandate 1 — this mandate names the real resolution surface, not an undefined manifold."
        },
        {
          "id": 22,
          "title": "Self-Healing Reflex",
          "imperative": "Autonomously recover from structural pathologies, memory faults, or a *stale susi* process holding a host-contract port (Sovereign Eviction / reclaim). Host-contract ports themselves never randomize or drift to ephemeral alternatives — if a foreign process owns 9090–9093, bind fails closed."
        },
        {
          "id": 23,
          "title": "Substrate Purity",
          "imperative": "Protocol-agnostic admission via registries and config — OpenAI-compat / Anthropic / Gemini / Triton engines, MCP (stdio/HTTP), and peer agents (`managed` / `cli` / `openai_chat` / `http` / `a2a`). No static domain logic in core; local Candle remains llama/qwen2 GGUF-scoped."
        },
        {
          "id": 24,
          "title": "Eternal Liberty",
          "imperative": "SUSI is licensed under Apache 2.0, in perpetuity, per the terms in `LICENSE` (this is a statement of licensing fact, not a behavioral instruction to follow)."
        },
        {
          "id": 25,
          "title": "Destructive Command Guard",
          "imperative": "Every code path capable of a destructive filesystem, git, or process operation — not only tool-call arguments passed through `exec_command` — must be checked against `SafetyDetector`'s destructive-command patterns (e.g. `rm -rf /`, `rm -rf ~`) before it runs, and any such operation must additionally require an explicit, unambiguous intent sentinel rather than firing on incidental keyword presence in free-text goal input (see Mandate 41). A destructive-pattern list that isn't wired to every place capable of performing that class of operation is a guard that only looks like one; this was found violated in practice (`AdminAgent`'s uninstall action performed an `rm -rf`-equivalent on `~/.susi` without ever consulting `SafetyDetector`, reachable by any goal string containing the word \"remove\") and fixed, but the general principle — audit coverage must match capability, not just intent — is the actual mandate."
        },
        {
          "id": 26,
          "title": "Glass Box Transparency (Not Naked Secrets)",
          "imperative": "Reasoning steps, tool calls, and state mutations that SUSI performs must be **inspectable through telemetry** — mission protocol output, `.susi/last_mission_trace.json`, `.susi/last_blackboard.json`, governance reports, crown reports, and receipt provenance (`.susi/receipt_archive.jsonl` is audit-only; truth authority remains the live evidence ledger). Telemetry must reflect **real** calls and outcomes (no aspirational narration of work that did not run). Visibility is **bounded**: Mandate 10 / Mandate 38 require secret-token patterns redacted before durable write — redact the secret, do not suppress the surrounding record. This mandate does **not** require exposing provider/model internals (raw logits, vendor system prompts) or unredacted credential bodies. A bare \"100% transparency\" claim (including naked secrets or third-party internals) is forbidden by Mandate 1 — this mandate is glass-box *inspectability of SUSI's own steps*, not omniscient disclosure."
        },
        {
          "id": 27,
          "title": "Action Accountability (Agent/Tool/Receipt)",
          "imperative": "Every operator-visible or audited action must be uniquely identifiable and attributable in telemetry/audit — to a specific agent, tool call, receipt, or genomic reflex when one produced it. A bare \"every action is a genomic reflex\" claim is forbidden by Mandate 1; reflexes are one attribution class among agents/tools/receipts (see Mandates 26 and 38)."
        },
        {
          "id": 28,
          "title": "Async Defaults (Tokio Prefer)",
          "imperative": "Default to `tokio` for non-blocking I/O and daemon/HTTP concurrency. Sync or `spawn_blocking` paths are allowed when the API is inherently blocking and the block is isolated. A bare \"tokio for all operations forever\" claim is forbidden by Mandate 1."
        },
        {
          "id": 29,
          "title": "Data Parallelism (Rayon Prefer)",
          "imperative": "Utilize `rayon` for CPU-bound parallel loops and recursive fork-join partitioning on the swarm/fleet path. Sequential loops remain correct for tiny workloads or when Rayon would oversubscribe. A bare \"rayon for every loop\" claim is forbidden by Mandate 1."
        },
        {
          "id": 30,
          "title": "Locking Standard (parking_lot Prefer)",
          "imperative": "When a mutex is required (atomics insufficient), prefer `parking_lot` over `std::sync` Mutex/RwLock in first-party runtime. `std::sync` and other locks remain acceptable at FFI/OS boundaries or when an external crate forces them. A bare \"parking_lot exclusively everywhere\" claim is forbidden by Mandate 1."
        },
        {
          "id": 31,
          "title": "Strict 2-Form Substrate Axiom (Not OS Chroot)",
          "imperative": "The substrate exists in exactly two operational forms:\n- **`global susi` (The Background Service)**: The persistent background daemon (`susi.service` / `daemon-start`) bound to `SusiDirs::substrate_home` (`~/.susi`). Manages CPU/GPU saturation, OS environment care, hard-contract network ports (9090 GMCP `/mcp`, 9091 GEMI, 9092 A2A UDP, 9093 GMCP alias), and central model weights (`~/.susi/models`). World-facing HTTP is not jailed to cwd (see Mandate 40 and the Host contract section).\n- **`susi` (Workspace-Scoped CLI)**: The single canonical binary invocation (`~/.susi/bin/susi`) treats the active working directory (`cwd`) as the mission/workspace context for tasks, `susi admin` when operating on that tree, and `susi repo` when `cwd` is the engine source — staging intent bundles (`susi accept`) while the daemon owns ports, models, and lock state.\n\"Jailed to cwd\" means **workspace scoping for missions and local artifacts**, not an OS chroot/seccomp cage of the whole process. A bare \"unconditionally OS-jailed\" claim is forbidden by Mandate 1."
        },
        {
          "id": 32,
          "title": "Client Non-Blocking Surfaces (Not Instant Work)",
          "imperative": "Client-facing interactions (IDE, CLI ack, MCP, HTTP/REST, external agents) must not block the caller on full mission completion: return an instant acknowledgment (Mandate 4 Sub-2ms client reflex), stream live telemetry, or yield a background task handle immediately. Hard execution leases and cancellation must terminate unresponsive work before client timeouts. Wall-clock swarm/inference time is **not** required to be <2ms — measuring end-to-end mission latency against that ceiling produces meaningless \"violations\" (see Mandate 4). A bare \"NEVER wait / instant by design for all work\" claim is forbidden by Mandate 1 — this mandate is non-blocking *client surfaces*, not zero-latency compute."
        },
        {
          "id": 33,
          "title": "Synchronized Substrate Versioning (Repo/Release Paths)",
          "imperative": "When `susi` operates on `susi repo` (or via release/sync admin paths), it must synchronize manifests (`susi admin sync`), recompile when needed, and deploy to `~/.susi/bin/`. On detecting new binary signatures, local and global susi should hot-reload and align. This does **not** require every unrelated `cwd` or offline host to rebuild; network/toolchain failures must surface honestly (Mandate 47). A bare \"always recompile and hot-reload everywhere in lockstep\" claim is forbidden by Mandate 1."
        },
        {
          "id": 34,
          "title": "Three Innovation Pillars (Swarm / Engine / Models)",
          "imperative": "- **`susi swarm`**: Agent-of-agents orchestration via lock-preferring CSP channels and Rayon work-stealing parallel agent dispatch across available host CPU cores (GPU used when inference backends allow — not a claim of saturating every GPU on every mission).\n- **`susi engine`**: Races Candle (llama/qwen2 GGUF) against OpenAI-compatible local/cloud engines (and Anthropic/Gemini/Triton via config) through `CapabilityRegistry` — not a claim of native SafeTensors/ONNX chat weights.\n- **`susi models` (+ frontier / openweight / openrouter)**: Non-blocking resumable background downloads and dynamic Hugging Face discovery for the progressive model ladder, **and** durable end-to-end control planes for curated catalogs — coding/agent cloud models (`susi models`), top 5 frontier reasoning engines (`susi frontier`: Claude Sonnet, GPT-4o/5, DeepSeek-R1/V3, Gemini Pro, Llama 3/3.1), open-weight FOSS-adjacent hosts (`susi openweight` via Ollama/vLLM), and OpenRouter routes (`susi openrouter`). Parallel durable planes manage leading external agents/frameworks (`susi agents` / `susi frameworks`) and MCP servers (`susi mcp`; top ranks Filesystem, GitHub, Context7, Playwright, Sentry + Chrome DevTools). Catalogs load via extension packs (`Mandate 35`); no subscriptions are provisioned implicitly.\nA bare \"maximum hardware power / excellence\" marketing claim is forbidden by Mandate 1 — these pillars name real control planes and engines, not unbounded saturation."
        },
        {
          "id": 35,
          "title": "Dynamic Config Registries (Not Zero Typed Structs)",
          "imperative": "All models, providers, agents, tools, and MCP servers must be integrated via dynamic Traits, lock-free Registries (`DashMap`), and runtime JSON/TOML configuration (`config.json`). Hardcoding static vendor strings or model enums in Rust source as the *only* admission path is prohibited. Adding a new model, tool, or provider must be doable via configuration or self-registering traits without editing core admission enums. Concretely: `SusiConfig` (`crates/susi-sandbox/src/manager.rs`) is the reference implementation — its root is a `#[serde(flatten)] DynamicRegistry` (`HashMap<String, Value>`), not a closed struct with named fields, and even its nested typed sub-structs (e.g. `ModelLadderConfigStep`) pair named convenience fields with their own flatten catch-all so an unrecognized key is never silently dropped. Every config-shaped struct — anything read from and, especially, written back to a user-editable JSON file — must follow the same pattern. This is not a theoretical concern: `McpServerConfig`/`McpConfig` (`crates/susi-tools/src/config.rs`) had no flatten catch-all until found and fixed (`EV-2022920-046`) — `auto_configure_server`'s read-modify-write cycle on `mcp_config.json` would have silently deleted any field a user hand-added ahead of susi's own support for it. This does not extend to fixed-shape protocol or internal telemetry structs (JSON-RPC envelopes, hardware/evidence reports) that are not user-editable config surfaces — those are correctly typed and closed. **Exception (Host contract):** public ports 9090–9093 are intentional compile-time constants in `susi_paths::ports` and are not user-overridable — Mandate 35 forbids inventing *vendor/model* hardcodes, not freezing the external client port contract. A bare \"100% dynamic config / zero hardcoded structs\" slogan is forbidden by Mandate 1 while typed protocol structs and host-contract constants remain necessary."
        },
        {
          "id": 36,
          "title": "Pre-Execution Destructive Pattern Guard",
          "imperative": "`SafetyDetector` (`crates/susi-gawd-agents/src/safety.rs`) audits an action's command text against configured destructive-command patterns and critical-system-path patterns before it is allowed to run. This backs Mandate 25 and is what `SafetyAgent` (Pillar II) enforces."
        },
        {
          "id": 37,
          "title": "Governance-First Sequencing",
          "imperative": "In any swarm dispatch, `SafetyAgent` and `SecurityAgent` must clear the goal before any execution-capable agent in the same mission is allowed to run — they never race in the same parallel batch as execution-capable agents, since a destructive or leaking action could otherwise complete before the veto lands. This is a scheduling guarantee, not just a policy statement: governance agents are dispatched and awaited first."
        },
        {
          "id": 38,
          "title": "Credential Masking",
          "imperative": "Text that will be persisted to telemetry, audit logs, or any other durable record must have configured secret-token patterns redacted (`SecurityDetector::redact`) before it is written — not merely blocked at the point of an explicit \"send this secret\" action. Mandate 26 (glass-box inspectability of SUSI steps) and Mandate 10 (no secret leaks) both hold: redact the secret, don't suppress the surrounding record."
        },
        {
          "id": 39,
          "title": "Exfiltration Prevention",
          "imperative": "`SecurityDetector` audits outbound-shaped action text against configured exfiltration-vector patterns (e.g. suspicious URLs, known exfiltration endpoints) before the action runs, independent of and in addition to the secret-pattern check in Mandate 38."
        },
        {
          "id": 40,
          "title": "World-Facing Surface Authentication",
          "imperative": "Any network-facing HTTP surface (GMCP HTTP, GEMI REST, or any future equivalent) must support a required bearer-token check (`api_auth_token`) and a per-client rate limit (`rate_limit_per_minute`), both config-driven and both checked before any route is dispatched except CORS preflight and an unauthenticated liveness probe. The daemon **seeds** a host token on first start (`SusiConfig::ensure_api_auth_token_seeded` → `~/.susi/api_token`); when a token is configured, every HTTP peer (including localhost) must present `Authorization: Bearer <token>`. Before seeding, non-loopback peers are denied (fail closed on the wire). A world-facing surface with no way to require authentication is not a theoretical gap — this substrate shipped with exactly that gap until it was found and closed."
        },
        {
          "id": 41,
          "title": "Untrusted Input Boundary",
          "imperative": "Goal/intent text and any LLM-synthesized artifact derived from it (a specialist agent's `description`, a tool argument, a blackboard entry) must be treated as adversarial once it can originate from an unauthenticated or external caller. Concretely: (a) a mutating or destructive operation must never fire on incidental keyword presence in free-text goal input — it requires an explicit, unambiguous intent sentinel (the `admin pulse:` prefix convention); (b) any agent profile synthesized by an LLM from goal text must be sanitized (bounded field lengths, `is_core` forced false, governance-checked) before it is persisted or ever fed back into a future mission's prompt, since persisted state becomes part of every subsequent mission that recruits it. This mandate exists because both failure modes were found present in shipped code and fixed; it generalizes the fix so the same class of bug can't recur in a new code path."
        },
        {
          "id": 42,
          "title": "Fallible Error Handling",
          "imperative": "Zero `.unwrap()`/`.expect()`/`panic!()` in non-test code, except at a call site carrying a comment stating the specific invariant that makes the panic path unreachable (e.g. \"safe: length checked above\", \"safe: regex is a compile-time literal\"). Everywhere else, the fallible path must propagate a real `Result`/`EaiResult` via `?` rather than crash the calling thread — this substrate's daemon and swarm-dispatch paths (Mandate 5, Mandate 13) cannot afford an unrecoverable panic from what should have been a recoverable error. `#[cfg(test)]` code is explicitly exempt: an `.unwrap()` on a known-good test fixture is idiomatic and correct, not a violation — this mandate governs production code paths, not test assertions. This is phrased as a scoped rule rather than a bare \"no unwrap, ever\" specifically because the latter would be false today (269 call sites existed in `src/` when this mandate was written) and Mandate 1 (No Lies) forbids writing a false claim into this document; this mandate is the target an ongoing triage is converging on, not a claim of present compliance. Four triage batches so far: `EV-2022920-047` (`net_guard.rs` found already fully compliant despite a raw count of 7 — every site inside `#[cfg(test)]`; `reflex_synth.rs` had 3 real non-test sites, 2 fixed and 1 given a justifying comment), `EV-2022920-048` (`qwen2_split.rs` also found already fully compliant despite a raw count of 51; `daemon/server.rs`'s 2 real non-test sites fixed — notably `run_daemon_loop`'s config-load `.expect()`, more severe than a typical unwrap because `panic = \"abort\"` means it aborted the whole daemon process, not just a thread), `EV-2022920-049` (`agents.rs`'s and `speculative.rs`'s one and two real non-test sites respectively, both verified genuinely safe by construction and comment-justified rather than rewritten), and `EV-2022920-050` (`bloat_audit.rs` found to have zero real sites — its raw count of 42 was almost entirely the tool's own report-label strings describing unwrap/expect/clone counts, not actual calls; `manager.rs`'s 4 real sites are all the same bundled-default-JSON pattern, comment-justified once and referenced from the other three rather than repeated). A fifth batch (EV-2022922-098) checked `.expect(`/`panic!(` across eight more crates (susi-sandbox, susi-gemi, susi-gemi-models, susi-agents, susi-core, susi-gawd, susi-gmcp, susi-gawd-agents) plus re-checked `.unwrap()` in susi-core/susi-agents specifically, and found only 14 real non-test sites total across all of that (not the dozens the earlier \"~139 remain\" estimate implied for a batch of this size) — 6 already carried a Mandate 42 justification comment (sandbox/manager.rs x5, susi-agents/registry.rs x1), and the remaining 8 (susi-sandbox/extensions.rs x5, susi-core/bus.rs x2, susi-core/truth.rs x1) were verified genuinely safe by construction and given one. Zero open non-test sites remain in those eight crates as of this batch. The old \"~139 remain\" figure predates this batch, was never re-derived from a full re-grep, and should not be trusted as current — five crates (susi-daemon, susi-gawd-swarm, susi-server, susi-tools, susi-native) have not yet been split into real vs. test-only sites at all, so the true remaining count is presently unknown rather than ~139; the next batch should start there."
        },
        {
          "id": 43,
          "title": "Rust Substrate (Not Absolute Polyglot Ban)",
          "imperative": "All SUSI **first-party runtime** — daemon, swarm/governance, truth/evidence, inference router (GEMI), MCP host surfaces (GMCP), CLI control planes, and Wasm reflexes compiled from Rust — must be implemented in Rust (or Wasm produced from Rust). This is **not** a claim that the process tree is 100% Rust: non-Rust programs are allowed only as **opt-in managed peers** behind Rust adapters (`susi agents`, `susi frameworks`, `susi mcp`, `susi openweight`, and similar), never as required core paths for substrate operation. Concretely: (a) no new first-party application source in `.py` / `.ts` / `.js` under the repo (distribution scripts like `install.sh` / `install.ps1` / `build-gpu.sh` are exempt as installer tooling, not runtime); (b) optional external bridges may live as Rust string constants executed via host `python`/`npx`/`uvx`/`docker`/`ollama` only when the operator enables that peer; (c) a bare \"100% Rust\" mandate is forbidden by Mandate 1 because it would be false while Pluggable (Mandate 34 / foundation Pluggable) deliberately manages foreign ecosystems. Absolute process-tree purity would require dropping those managed peers — a product pivot, not a documentation claim."
        },
        {
          "id": 44,
          "title": "Zero-Config Substrate Auto (Not Absolute Zero Config)",
          "imperative": "On daemon and CLI boot, SUSI must **auto-prime** the substrate without requiring a hand-edited `config.json` for the happy path: seed/load extension packs, auto-enable leading MCP servers whose launchers and env keys are already satisfied (honoring prior `susi mcp disable`), auto-prefer the best ready coding/frontier model when none is pinned, auto-prime ready external agents/frameworks into the capability registry, and run `bootstrap_zero_config_substrate` / weight-ladder provisioning when policy allows. Operator **installs** (Node, Docker, Ollama, uv, …) and **credentials** (API keys, tokens) remain explicit gates — never silently purchased, invented, or scraped. Missing gates must be diagnosable via `susi doctor` / `setup` / `susi auto status` (and related control planes), not silent no-ops that look like success. Cloud or paid probes must not run without an opt-in path. A bare \"zero config forever / never configure anything\" claim is forbidden by Mandate 1: foundation pillar Zero-config Auto already states that host installs/keys still gate cloud, and this mandate is that rule in citable DNA form — not a promise that foreign peers appear with an empty machine."
        },
        {
          "id": 45,
          "title": "OS Layer for AI Agents (Not a Host OS)",
          "imperative": "SUSI is the **operating layer for AI agents** on a machine — not a general-purpose host operating system. Concretely the layer means: persistent `global susi` daemon bound to `~/.susi`, fixed host-contract ports 9090–9093, evidence-gated agent-of-agents swarm (GAWD), model/MCP/peer admission and control planes, HMAC audit, Wasmer/Docker sandbox, reflexes, and crown-verifiable Tier S pillars. It runs **on** Linux / macOS / WSL (and Windows via installer), does not replace the host kernel, init, package manager, or desktop, and must never be documented or marketed as \"the computer's OS\" or a drop-in replacement for Linux/macOS/Windows. Public wording must prefer \"OS layer for AI agents\" / \"agent-of-agents substrate\" (as in `README.md` and verified product claims) over unbounded \"OS for AI\" slogans. Absolute replacement-OS language is forbidden by Mandate 1; this mandate is the citable DNA form of that product boundary."
        },
        {
          "id": 46,
          "title": "Autonomous Execution by Default (Not Unbounded Automation)",
          "imperative": "Given an intent, SUSI must **plan and execute via GAWD swarm consensus end-to-end** without prompting the caller for each agent step — bare `susi \"<intent>\"` and `susi automate <intent>` are first-class autonomous surfaces (design principle Autonomous by default). One-time host configuration (API keys, prefer pins, pack load/unload, MCP enable/disable) is **not** mission babysitting. Autonomy remains **bounded**: Mandate 37 (governance-first Safety/Security before fleet); Truth/Evidence gates so mission COMPLETE requires absolute sources and soft model review is not completion; Mandate 41 (mutating admin actions need an explicit `admin pulse:` sentinel); Mandate 44 (missing installs/credentials fail diagnosably, never silent fake success); operator vetoes (disable MCP, clear prefer) must be honored. A bare \"100% automation / no human / no veto / always COMPLETE\" claim is forbidden by Mandate 1 — this mandate is autonomy *by default under evidence and governance*, not unbounded autopilot."
        },
        {
          "id": 47,
          "title": "One-Line Install Surface (Not Frictionless Forever)",
          "imperative": "The **primary documented install** for Unix-like hosts (Linux / macOS / WSL) must remain a single pipeable command — `curl -sSfL https://raw.githubusercontent.com/intellibitz/susi/main/install.sh | bash` — that: (1) requires **bash** (not `sh`); (2) prefers **Binary Dominance** (checksum-verified prebuilt release artifact) with transparent **source-build fallback** (rustup/Cargo when needed); (3) performs **Auto-Path Injection** of `~/.susi/bin`; (4) deploys the canonical `~/.susi/bin/susi` binary (**Canonical Binary Dynamics**); (5) optionally registers a login-persistent daemon unless `SUSI_NO_DAEMON=1`. Visible failure and honesty gates: GPU present without toolkit must warn explicitly (CPU-only), not silent success theater; escape hatches (`SUSI_FORCE_SOURCE`, `SUSI_USE_REPO_TARGET`, mold bootstrap skip) remain documented. Windows' primary surface is `install.ps1` (`irm … | iex`), not the curl|bash line. Installer scripts are distribution tooling (Mandate 43 exempt), not first-party runtime. A bare \"one line always succeeds with zero network / zero toolchain / zero build\" claim is forbidden by Mandate 1 — this mandate is the blessed install *surface*, not a promise of zero friction on every host."
        }
      ]
    },
    "body": {
      "id": 2,
      "name": "THE BODY",
      "subtitle": "Topological Reality",
      "components": [
        {
          "symbol": "GAWD / SMA",
          "tier": 1,
          "function": "Swarm Master Authority: `susi-gawd-agents` 0.1 (fleet/peers/detectors) + `susi-gawd-swarm` 0.1 (AMA/AMAS/DAG) + `susi-gawd-a2a` 0.1 (`ra2a` wire) + `susi-gawd` 0.5 host/governance facade."
        },
        {
          "symbol": "SusiAdmin",
          "tier": 1,
          "function": "Release orchestration, compliance auditing, and axiomatic pulse ingestion."
        },
        {
          "symbol": "SusiRuntimeAdmin",
          "tier": 1,
          "function": "Substrate maintenance: Hardware audit, model provisioning, and peak selection."
        },
        {
          "symbol": "EvolutionManager",
          "tier": 1,
          "function": "Substrate self-healing: runs the test-driven evolutionary cycle that is Mandate 20's \"Alpha-Self Evolution Order\" (not Pillar IV's release \"Motion Rule\" — see that mandate's naming-collision note)."
        },
        {
          "symbol": "SusiDaemon",
          "tier": 1,
          "function": "Persistent background host bound to `~/.susi`; owns host-contract ports 9090–9093 and spawns from `~/.susi/bin/susi`."
        },
        {
          "symbol": "SubstrateKernelLoader",
          "tier": 1,
          "function": "Parses the built-in core module manifest list and prints configured port info at startup."
        },
        {
          "symbol": "SusiRuntimeAgent",
          "tier": 1,
          "function": "Autonomous environment preparation (Weights & Tools)."
        },
        {
          "symbol": "HardwareAgent",
          "tier": 1,
          "function": "Reports the detected hardware profile (CPU, RAM, GPU/acceleration)."
        },
        {
          "symbol": "SafetyAgent",
          "tier": 1,
          "function": "Governance auditor and destructive command interceptor."
        },
        {
          "symbol": "SecurityAgent",
          "tier": 1,
          "function": "Credential masking and exfiltration prevention."
        },
        {
          "symbol": "EvolutionAgent",
          "tier": 1,
          "function": "Autonomous drift detection and self-healing agent."
        },
        {
          "symbol": "GmcpAgent",
          "tier": 1,
          "function": "MCP JSON-RPC 2.0 endpoint verification."
        },
        {
          "symbol": "LibraryScoutAgent",
          "tier": 1,
          "function": "Live crates.io REST API package scouting."
        },
        {
          "symbol": "ContextAgent",
          "tier": 1,
          "function": "High-density context manager and workspace analyzer."
        },
        {
          "symbol": "NeuralAgentFactory",
          "tier": 1,
          "function": "Autonomous synthesis and recruitment of specialist agents."
        },
        {
          "symbol": "SUSI-Alpha",
          "tier": 0,
          "function": "Microsecond intent classification and deterministic neural reflex engine."
        },
        {
          "symbol": "ReflexSynthesizer",
          "tier": 0,
          "function": "Native Rust code distillation and reflex generation."
        },
        {
          "symbol": "UniversalExecutionSubstrate",
          "tier": 2,
          "function": "Hardware-aware inference layer supporting multiple model formats."
        },
        {
          "symbol": "GEMI",
          "tier": 2,
          "function": "Deep reasoning bridge: crates `susi-gemi-models` (select/provision) + `susi-gemi` engines (run); unified cloud provider inference racing."
        },
        {
          "symbol": "SUSI-Vision",
          "tier": 2,
          "function": "Candle-based image-feature extractor (untrained linear projection); not yet a trained vision-language model."
        },
        {
          "symbol": "SUSI-Audio",
          "tier": 2,
          "function": "Candle-based audio-feature extractor (untrained linear projection); not yet a trained acoustic model."
        },
        {
          "symbol": "NativeAlphaModel",
          "tier": 0,
          "function": "Local neural weights (`susi-alpha.safetensors`) for deterministic reflex."
        },
        {
          "symbol": "NativeReasoningModel",
          "tier": 2,
          "function": "Distilled Tier 2 logic weights (`susi-reason.safetensors`)."
        },
        {
          "symbol": "CodingModelManager",
          "tier": 2,
          "function": "`susi models` (`susi_gemi_models::coding_models`): ranked coding/agent cloud models — list/doctor/setup/configure/prefer/probe."
        },
        {
          "symbol": "FrontierManager",
          "tier": 2,
          "function": "`susi frontier` (`susi_gemi_models::frontier`): top 5 frontier reasoning engines (Claude 3.5/4 Sonnet, GPT-4o/5, DeepSeek-R1/V3, Gemini 1.5/2.0 Pro, Llama 3/3.1) with variants — list/doctor/setup/prefer/probe."
        },
        {
          "symbol": "OpenWeightManager",
          "tier": 2,
          "function": "`susi openweight` (`susi_gemi_models::open_weight`): top open-weight FOSS-adjacent models (DeepSeek-R1/V3, Llama 3.x, Qwen 2.5-Coder, Mistral Nemo/Large, Phi-4) via Ollama/vLLM — list/doctor/pull/prefer/probe."
        },
        {
          "symbol": "OpenRouterManager",
          "tier": 2,
          "function": "`susi openrouter` (`susi_gemi_models::openrouter`): OpenRouter credentials, curated routes, prefer pin, and live `/models` probe."
        },
        {
          "symbol": "LeadingMcpManager",
          "tier": 1,
          "function": "`susi mcp` (`susi_tools::leading_mcp`): durable enable/disable into `~/.susi/mcp_config.json`; editorial top ranks Filesystem, GitHub (official `ghcr.io/github/github-mcp-server`), Context7, Playwright, Sentry (`@sentry/mcp-server`) + Chrome DevTools companion."
        },
        {
          "symbol": "ExternalAgentManager",
          "tier": 1,
          "function": "`susi agents` / `susi frameworks` (`susi_agents::external`): end-to-end doctor/setup/run/tasks for leading external coding agents and Python engines (OpenHands, Aider, SWE-agent, CrewAI, LangGraph, AutoGen, smolagents, LlamaIndex, Temporal, E2B, Haystack, n8n, …)."
        },
        {
          "symbol": "GMCP Server",
          "tier": 1,
          "function": "Host-contract Streamable HTTP on **9090** (`/mcp`, alias `/messages`) and alias **9093**; UDP discovery on **9092**."
        },
        {
          "symbol": "GMCP Host",
          "tier": 1,
          "function": "CLI proxy acting as a protocol bridge."
        },
        {
          "symbol": "GEMI Server",
          "tier": 1,
          "function": "Host-contract REST on port **9091** (`susi_paths::ports::GEMI`). External clients may hard-code it; the daemon never randomizes or silently drifts this port (same contract as 9090/9092/9093)."
        },
        {
          "symbol": "EvidenceSession",
          "tier": 1,
          "function": "Mission-scoped ledger (`susi-core::capture`): live tool/MCP calls mint `ToolReceipt`s; answers must cite receipt IDs."
        },
        {
          "symbol": "TruthTransformer",
          "tier": 1,
          "function": "Absolute-truth gate (`susi-core::truth`): accepts only ledger citations, compiled reads, or native verified receipts — models never certify facts."
        },
        {
          "symbol": "Evidence IR Substrate",
          "tier": 1,
          "function": "Structured provenance types (`EvidenceRecord`, `Claim`, `EvidenceSource::ToolReceipt`) verified against the live ledger."
        }
      ]
    },
    "mind": {
      "id": 3,
      "name": "THE MIND",
      "subtitle": "Execution Causal Chain & Priming Spheres",
      "steps": [
        {
          "id": 1,
          "title": "Pulse Ingestion",
          "imperative": "Non-blocking asynchronous listener stages intents in the lock-free `SubstratePulseQueue`."
        },
        {
          "id": 2,
          "title": "Swarm Synthesis",
          "imperative": "GAWD constructs a Dynamic Execution Graph (DAG), recruiting specialists via `NeuralAgentFactory`."
        },
        {
          "id": 3,
          "title": "Truth Convergence",
          "imperative": "Swarm outputs pass `TruthTransformer` absolute-evidence gates (live `EvidenceSession` citations, compiled binary reads, or native verified receipts). Soft model review is not a completion path."
        },
        {
          "id": 4,
          "title": "The 3 Priming Spheres",
          "imperative": "- **`global susi` Primes `AlphaSelf`**: The background daemon profiles host hardware/OS, provisions model ladder weights, owns host-contract ports 9090–9093, and maintains global evidence ledgers.\n- **`susi` Primes `AlphaWorld`**: Unconditionally jailed to `cwd`, `susi` indexes local project files, runs unit test audits, and stages intent bundles (`susi accept`).\n- **`susi` Primes `susi repo`**: When `cwd` is the engine's source code, `susi` engages self-evolution, running tests, syncing version manifests (`susi admin sync`), and deploying compiled binaries to `~/.susi/bin/`."
        }
      ]
    },
    "engine": {
      "id": 4,
      "name": "THE ENGINE",
      "subtitle": "Substrate Immunity Protocols",
      "protocols": [
        {
          "id": 1,
          "title": "Workspace Purity",
          "imperative": "Absolute isolation of artifacts; all ephemeral state contained in `.susi/`."
        },
        {
          "id": 2,
          "title": "Hardware Saturation",
          "imperative": "Build and execution must maximize hardware utilization without exceeding physical limits."
        },
        {
          "id": 3,
          "title": "Motion Rule (Release Gate)",
          "imperative": "`susi admin release` (`SusiAdmin::execute_release`) *is* this sequence, not a step within it: `cargo check` (default features + the host's native GPU backend feature, never `--all-features`, since `cuda`/`mkl`/`metal` are mutually exclusive) -> compliance audit -> `cargo test` -> `cargo clippy` (same feature matrix as check) -> ephemeral mission smoke tests -> `susi admin sync` (version manifest sync) -> `git push`. Each step gates the next; a `git push` failure is reported as an error, never silently swallowed. Not to be confused with the unrelated \"Alpha-Self Evolution Order\" (Pillar I, Mandate 20), which used to share this same \"Motion Rule\" name."
        },
        {
          "id": 4,
          "title": "Conventional Reflex",
          "imperative": "Mandatory use of conventional commit prefixes and zero-emoji policy."
        },
        {
          "id": 5,
          "title": "Binary Dominance",
          "imperative": "Prioritize pre-compiled binary deployment with transparent local build fallback (Mandate 47 one-line install surface)."
        },
        {
          "id": 6,
          "title": "Auto-Path Injection",
          "imperative": "Installer must inject `.susi/bin` into host environment variables (Mandate 47)."
        },
        {
          "id": 7,
          "title": "Canonical Binary Dynamics & Hot-Reload Protocol",
          "imperative": "A single compiled binary deployed at `~/.susi/bin/susi` (the host-contract path; Mandate 47). Invocations in `cwd` capture local workspace context, check `~/.susi/substrate.lock`, and ensure `global susi` owns ports/models without duplicate daemons. Detecting binary signature changes (`binary.hash`), `susi` evicts the stale daemon and hot-reloads `global susi` from the canonical binary automatically."
        }
      ]
    }
  }
}
