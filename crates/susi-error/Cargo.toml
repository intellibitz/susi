[package]
name = "susi-error"
version = "0.1.0"
license = "Apache-2.0"
edition = "2021"
publish = false

[dependencies]
serde_json = { workspace = true }
candle-core = "0.11.0"
susi-paths = { path = "../susi-paths" }
