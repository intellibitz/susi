[package]
name = "susi-tools"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dashmap = "6.0"
parking_lot = "0.12"
libc = "0.2"
susi-error = { path = "../susi-error" }
susi-paths = { path = "../susi-paths" }
susi-sandbox = { path = "../susi-sandbox" }
susi-native = { path = "../susi-native" }
